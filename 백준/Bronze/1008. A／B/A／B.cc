#include <iostream>
#include <iomanip>
#include <ctime>
using namespace std;

int main(void) {
    double A, B;
    cin >> A >> B;
    cout.precision(10);
    cout << A / B;
    return 0;
}
