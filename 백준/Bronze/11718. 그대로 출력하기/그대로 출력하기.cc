#include <iostream>
#include <iomanip>
#include <cstdlib>
#include <ctime>
#include <array>
#include <vector>
#include <algorithm>
#include <string>
//#include "Rikkabrary.cpp"

using namespace std;



int main(void) {
    cin.tie(NULL);
    ios_base::sync_with_stdio(false);

    string word;
    
    while (getline(cin, word)) {
        cout << word << '\n';
    }

    

    return 0;
}
